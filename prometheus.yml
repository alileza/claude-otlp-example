global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: 'claude-code-otlp'
    static_configs:
      - targets: ['otel-collector:8889']  # OpenTelemetry collector Prometheus endpoint
    scrape_interval: 10s
    metrics_path: /metrics